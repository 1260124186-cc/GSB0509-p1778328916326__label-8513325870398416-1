# 数据准备模块 - 使用说明

## 一、项目简介

这是一个基于 Vue 3 + TypeScript + Vite 开发的数据准备模块，提供 Excel 数据的导入、展示、统计和编辑功能。

## 二、核心功能

### 1. Excel 导入
- 支持 `.xlsx` 和 `.xls` 格式
- 拖拽上传或点击选择文件
- 文件大小限制：10MB
- 自动解析表头和数据

### 2. 数据表格展示
- 基于 VXETable 的强大表格组件
- 支持列排序、筛选
- 支持列宽调整
- 斑马纹和悬停高亮
- 虚拟滚动，支持大数据量

### 3. 数据统计
实时显示以下统计信息：
- 文件名和导入时间
- 数据行数和列数
- 所有列名（表头）
- 各列的数据类型（数字、文本、日期、空）
- 数据类型分布统计

### 4. 数据编辑
- 双击单元格即可编辑
- 支持添加新行
- 支持删除单行或批量删除
- 实时更新统计信息
- 导出编辑后的数据为 Excel

## 三、技术栈

### 核心框架
- **Vue 3.4+**: 使用 Composition API
- **TypeScript 5.3+**: 类型安全
- **Vite 5.0+**: 极速开发体验

### UI 组件
- **Element Plus 2.5+**: 企业级 UI 组件库
- **VXETable 4.5+**: 强大的表格组件
- **Element Plus Icons**: 图标库

### 状态管理与路由
- **Pinia 2.1+**: Vue 官方状态管理库
- **Vue Router 4.2+**: 路由管理

### 工具库
- **xlsx 0.18+**: Excel 文件处理
- **Axios 1.6+**: HTTP 客户端（预留）

## 四、项目结构

```
data-preparation-module/
├── src/
│   ├── views/                          # 页面
│   │   └── DataPreparation/            # 数据准备模块
│   │       ├── index.vue               # 主页面
│   │       ├── components/             # 子组件
│   │       │   ├── ExcelImporter.vue   # Excel 导入组件
│   │       │   ├── DataTable.vue       # 数据表格组件
│   │       │   └── DataStatistics.vue  # 数据统计组件
│   │       └── composables/            # 组合式函数
│   │           └── useTableEdit.ts     # 表格编辑逻辑
│   ├── stores/                         # Pinia Store
│   │   └── dataPreparation.ts          # 数据准备模块状态
│   ├── types/                          # TypeScript 类型
│   │   └── dataPreparation.ts          # 类型定义
│   ├── utils/                          # 工具函数
│   │   └── excelUtils.ts               # Excel 处理工具
│   ├── router/                         # 路由配置
│   │   └── index.ts                    # 路由定义
│   ├── App.vue                         # 根组件
│   ├── main.ts                         # 应用入口
│   └── vite-env.d.ts                   # Vite 类型声明
├── .vscode/                            # VSCode 配置
│   ├── extensions.json                 # 推荐扩展
│   └── settings.json                   # 编辑器设置
├── index.html                          # HTML 入口
├── package.json                        # 依赖配置
├── tsconfig.json                       # TypeScript 配置
├── tsconfig.node.json                  # Node TypeScript 配置
├── vite.config.ts                      # Vite 配置
├── .gitignore                          # Git 忽略文件
└── README.md                           # 项目说明
```

## 五、快速开始

### 1. 安装依赖

```bash
npm install
```

### 2. 启动开发服务器

```bash
npm run dev
```

服务器将在 `http://localhost:3000` 启动，并自动打开浏览器。

### 3. 构建生产版本

```bash
npm run build
```

构建产物将输出到 `dist` 目录。

### 4. 预览生产版本

```bash
npm run preview
```

## 六、使用流程

### 步骤 1：导入 Excel 文件

1. 在左侧"Excel 导入"区域，点击或拖拽上传 Excel 文件
2. 文件选择后会显示文件名和大小
3. 点击"开始导入"按钮进行导入
4. 导入成功后，数据会自动显示在右侧表格中

### 步骤 2：查看数据统计

导入成功后，页面会显示：
- **基础信息**: 文件名、导入时间、行数、列数
- **列信息**: 所有列名（表头）及其数据类型
- **类型分布**: 各种数据类型的统计

### 步骤 3：编辑数据

**编辑单元格**：
- 双击任意单元格即可编辑
- 编辑完成后点击其他地方或按 Enter 保存

**添加行**：
- 点击表格上方的"添加行"按钮
- 新行会添加到表格末尾

**删除行**：
- 单行删除：点击行尾的"删除"按钮
- 批量删除：勾选多行后，点击"删除选中"按钮

### 步骤 4：导出数据

1. 点击表格上方的"导出 Excel"按钮
2. 系统会自动下载编辑后的数据为 Excel 文件
3. 文件名格式：`导出数据_时间戳.xlsx`

## 七、代码结构说明

### 1. 类型定义 (`src/types/dataPreparation.ts`)

定义了模块中使用的所有 TypeScript 类型：
- `TableColumn`: 表格列配置
- `TableRow`: 表格数据行
- `DataStatistics`: 统计信息
- `ExcelImportConfig`: 导入配置
- `DataPreparationState`: 模块状态

### 2. 工具函数 (`src/utils/excelUtils.ts`)

Excel 处理相关的工具函数：
- `readExcelFile()`: 读取 Excel 文件
- `exportToExcel()`: 导出数据为 Excel
- `analyzeDataTypes()`: 分析各列数据类型

### 3. 状态管理 (`src/stores/dataPreparation.ts`)

使用 Pinia 管理应用状态：
- **State**: 表格数据、列配置、统计信息等
- **Getters**: 计算属性，如是否有数据、行列数等
- **Actions**: 数据操作方法，如添加、删除、更新等

### 4. 组件说明

**ExcelImporter.vue** - Excel 导入组件
- 文件上传（拖拽/点击）
- 文件验证（格式、大小）
- 导入处理和反馈

**DataTable.vue** - 数据表格组件
- 基于 VXETable 实现
- 支持编辑、添加、删除
- 工具栏操作（导出、刷新）

**DataStatistics.vue** - 数据统计组件
- 显示基础统计信息
- 列信息展示
- 数据类型分布

**index.vue** - 主页面组件
- 整合所有子组件
- 响应式布局
- 统一的事件处理

### 5. 组合式函数 (`src/views/DataPreparation/composables/useTableEdit.ts`)

封装表格编辑相关的逻辑：
- 编辑状态管理
- 单元格更新
- 行操作（添加、删除）
- 数据刷新

## 八、最佳实践

### 1. Excel 文件准备

- 第一行应为表头（列名）
- 避免合并单元格
- 数据应连续，避免中间有空行
- 建议文件大小不超过 10MB

### 2. 数据编辑

- 编辑前建议先导出备份
- 大批量修改建议在 Excel 中完成后重新导入
- 注意数据类型的一致性

### 3. 性能优化

- VXETable 支持虚拟滚动，可处理大量数据
- 如果数据量特别大（10万+行），建议分批处理
- 编辑后及时导出，避免浏览器内存占用过高

## 九、常见问题

### Q1: 导入失败怎么办？

**检查项**：
- 文件格式是否为 `.xlsx` 或 `.xls`
- 文件是否损坏
- 文件大小是否超过 10MB
- Excel 是否有密码保护

### Q2: 编辑的数据会自动保存吗？

**答**：不会。编辑的数据保存在浏览器内存中，刷新页面会丢失。请及时点击"导出 Excel"保存修改。

### Q3: 支持哪些数据类型？

**答**：系统会自动识别以下类型：
- 数字：纯数字的列
- 日期：可解析为日期的列
- 文本：其他所有列
- 空：没有数据的列

### Q4: 可以导入多个工作表吗？

**答**：当前版本只支持导入第一个工作表。如需导入其他工作表，请先在 Excel 中将其移到第一个位置。

### Q5: 表格能否支持更多操作？

**答**：VXETable 功能强大，后续可扩展：
- 列的隐藏/显示
- 数据筛选和搜索
- 复制/粘贴
- 撤销/重做
- 数据校验
- 公式计算

## 十、扩展建议

如需进一步扩展功能，可以考虑：

1. **数据验证**: 添加单元格数据验证规则
2. **数据转换**: 支持数据格式转换和清洗
3. **批量操作**: 支持批量替换、批量填充
4. **历史记录**: 支持撤销/重做操作
5. **协同编辑**: 支持多人同时编辑
6. **数据可视化**: 集成 ECharts 进行数据分析和图表展示
7. **API 集成**: 连接后端 API，支持数据持久化
8. **权限管理**: 添加用户权限控制

## 十一、技术支持

如遇到问题或需要帮助：

1. 查看浏览器控制台的错误信息
2. 检查 `README.md` 和本使用说明
3. 查看项目 Issues
4. 提交新的 Issue 描述问题

---

**祝使用愉快！** 🎉

