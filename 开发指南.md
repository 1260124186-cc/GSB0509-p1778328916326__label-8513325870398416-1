# 数据准备模块 - 开发指南

## 代码架构设计

### 1. 模块化设计原则

本项目采用高度模块化的设计，遵循以下原则：

#### 1.1 单一职责原则
- 每个组件只负责一个功能
- 工具函数按功能分类
- Store 只管理相关状态

#### 1.2 组件拆分策略
```
主页面 (index.vue)
├── Excel 导入组件 (ExcelImporter.vue)
├── 数据统计组件 (DataStatistics.vue)
└── 数据表格组件 (DataTable.vue)
```

#### 1.3 逻辑复用
- 使用 Composables 封装可复用逻辑
- 工具函数独立成模块
- 类型定义统一管理

### 2. 文件组织结构

```
src/
├── views/                    # 视图层
│   └── DataPreparation/      # 数据准备模块
│       ├── index.vue         # 主页面（容器组件）
│       ├── components/       # UI 组件
│       └── composables/      # 组合式函数（业务逻辑）
├── stores/                   # 状态管理层
├── types/                    # 类型定义层
├── utils/                    # 工具函数层
└── router/                   # 路由层
```

### 3. 数据流设计

```
用户操作 → 组件事件 → Composable → Store → 组件更新
         ↑                                    ↓
         └────────────── 响应式状态 ───────────┘
```

## 核心模块详解

### 1. 类型系统 (`src/types/dataPreparation.ts`)

#### 设计思路
- 所有数据结构都有明确的类型定义
- 使用 TypeScript 确保类型安全
- 便于 IDE 智能提示和错误检查

#### 主要类型

```typescript
// 表格列定义
interface TableColumn {
  field: string        // 字段名（唯一标识）
  title: string        // 显示标题
  minWidth?: number    // 最小宽度
  editRender?: any     // 编辑器配置
}

// 表格行数据
interface TableRow {
  [key: string]: any   // 动态字段
}

// 统计信息
interface DataStatistics {
  totalRows: number              // 行数
  totalColumns: number           // 列数
  columns: string[]              // 列名数组
  fileName: string               // 文件名
  importTime: string             // 导入时间
  dataTypes: Record<string, string>  // 数据类型
}
```

### 2. Excel 工具 (`src/utils/excelUtils.ts`)

#### 核心功能

**2.1 读取 Excel 文件**

```typescript
export const readExcelFile = (
  file: File,
  config: ExcelImportConfig = {}
): Promise<{ data: TableRow[]; columns: TableColumn[] }>
```

实现流程：
1. 使用 FileReader 读取文件
2. 通过 xlsx 库解析工作簿
3. 提取表头和数据行
4. 转换为标准格式
5. 返回结构化数据

**2.2 导出 Excel 文件**

```typescript
export const exportToExcel = (
  data: TableRow[],
  columns: TableColumn[],
  fileName: string
)
```

实现流程：
1. 准备导出数据（表头 + 数据行）
2. 创建工作表和工作簿
3. 设置列宽等样式
4. 触发浏览器下载

**2.3 数据类型分析**

```typescript
export const analyzeDataTypes = (
  data: TableRow[],
  columns: TableColumn[]
): Record<string, string>
```

识别逻辑：
- 全部为数字 → "数字"
- 全部可解析为日期 → "日期"
- 其他 → "文本"
- 无数据 → "空"

### 3. 状态管理 (`src/stores/dataPreparation.ts`)

#### 设计理念
- 使用 Pinia 替代 Vuex，更简洁的 API
- 状态集中管理，便于维护
- 支持 TypeScript 类型推断

#### Store 结构

```typescript
{
  // State - 状态
  tableData: TableRow[]           // 表格数据
  tableColumns: TableColumn[]     // 列配置
  statistics: DataStatistics | null  // 统计信息
  loading: boolean                // 加载状态
  fileName: string                // 文件名

  // Getters - 计算属性
  hasData: boolean                // 是否有数据
  rowCount: number                // 行数
  columnCount: number             // 列数
  columnNames: string[]           // 列名数组

  // Actions - 方法
  setTableData()                  // 设置数据
  updateStatistics()              // 更新统计
  updateCell()                    // 更新单元格
  addRow()                        // 添加行
  deleteRow()                     // 删除行
  clearData()                     // 清空数据
}
```

#### 关键方法实现

**更新统计信息**
```typescript
updateStatistics() {
  // 1. 检查数据是否存在
  // 2. 分析数据类型
  // 3. 生成统计信息
  // 4. 更新 statistics 状态
}
```

### 4. 组件设计

#### 4.1 ExcelImporter.vue - 导入组件

**职责**：
- 文件上传界面
- 文件验证（格式、大小）
- 调用工具函数解析
- 更新 Store 状态

**关键特性**：
- 拖拽上传支持
- 实时文件信息显示
- 加载状态反馈
- 错误处理

**核心代码**：
```typescript
const handleImport = async () => {
  loading.value = true
  store.setLoading(true)
  
  try {
    // 1. 解析 Excel 文件
    const { data, columns } = await readExcelFile(currentFile.value)
    
    // 2. 更新 Store
    store.setTableData(data, columns, currentFile.value.name)
    
    // 3. 成功提示
    ElMessage.success(`成功导入 ${data.length} 行数据`)
  } catch (error) {
    // 错误处理
    ElMessage.error(error.message || '导入失败')
  } finally {
    loading.value = false
    store.setLoading(false)
  }
}
```

#### 4.2 DataTable.vue - 表格组件

**职责**：
- 数据表格展示
- 单元格编辑
- 行操作（添加、删除）
- 数据导出

**技术选型**：
- 使用 VXETable 而非 Element Plus Table
- 原因：VXETable 功能更强大，性能更好
  - 虚拟滚动支持大数据量
  - 内置编辑功能
  - 丰富的配置选项

**编辑实现**：
```typescript
// 配置编辑模式
:edit-config="{ trigger: 'dblclick', mode: 'cell' }"

// 监听编辑完成事件
@edit-closed="handleEditClosed"

// 处理编辑
const handleEditClosed = ({ row, column }) => {
  const rowIndex = props.tableData.findIndex(r => r === row)
  emit('update', rowIndex, column.field, row[column.field])
}
```

#### 4.3 DataStatistics.vue - 统计组件

**职责**：
- 显示数据统计信息
- 列信息展示
- 数据类型分布

**设计亮点**：
- 使用 Element Plus Descriptions 组件展示结构化信息
- 标签颜色区分不同类型
- 响应式布局

**类型分布统计**：
```typescript
const typeDistribution = computed(() => {
  const distribution: Record<string, number> = {}
  Object.values(props.statistics.dataTypes).forEach(type => {
    distribution[type] = (distribution[type] || 0) + 1
  })
  return distribution
})
```

#### 4.4 index.vue - 主页面组件

**职责**：
- 组件布局和组织
- 事件协调
- 全局操作（清空数据）

**布局设计**：
```vue
<el-row :gutter="20">
  <!-- 左侧：导入 -->
  <el-col :md="8" :lg="6">
    <excel-importer />
  </el-col>
  
  <!-- 右侧：统计和表格 -->
  <el-col :md="16" :lg="18">
    <data-statistics />
    <data-table />
  </el-col>
</el-row>
```

### 5. Composables - 组合式函数

#### useTableEdit.ts

**作用**：
- 封装表格编辑相关逻辑
- 解耦组件和 Store
- 提供统一的编辑接口

**设计模式**：
```typescript
export const useTableEdit = () => {
  const store = useDataPreparationStore()
  
  // 封装 Store 方法
  const updateCell = (rowIndex, field, value) => {
    store.updateCell(rowIndex, field, value)
  }
  
  // 返回接口
  return {
    updateCell,
    addRow: store.addRow,
    deleteRow: store.deleteRow,
    refresh: store.updateStatistics
  }
}
```

**优势**：
- 业务逻辑与组件分离
- 便于测试
- 便于复用

## 样式设计

### 1. 样式组织

- 使用 SCSS 预处理器
- 组件内使用 scoped 样式
- 响应式设计

### 2. 主题色

遵循 Element Plus 默认主题：
- Primary: `#409eff`
- Success: `#67c23a`
- Warning: `#e6a23c`
- Danger: `#f56c6c`
- Info: `#909399`

### 3. 响应式断点

```scss
// 移动设备
@media (max-width: 768px) {
  // 单列布局
}

// 平板设备
@media (min-width: 768px) and (max-width: 1024px) {
  // 调整间距
}

// 桌面设备
@media (min-width: 1024px) {
  // 标准布局
}
```

## 开发规范

### 1. 命名规范

- **组件**: PascalCase，如 `DataTable.vue`
- **文件夹**: camelCase 或 kebab-case
- **变量/函数**: camelCase，如 `handleImport`
- **常量**: UPPER_SNAKE_CASE
- **类型**: PascalCase，如 `TableColumn`

### 2. 代码风格

- 使用 TypeScript 严格模式
- 函数优先使用箭头函数
- 使用 `const` 和 `let`，避免 `var`
- 组件使用 `<script setup>` 语法

### 3. 注释规范

```typescript
/**
 * 函数说明
 * @param file - 参数说明
 * @returns 返回值说明
 */
export const readExcelFile = (file: File) => {
  // 实现代码
}
```

### 4. 提交规范

```
feat: 新功能
fix: 修复
docs: 文档
style: 格式
refactor: 重构
test: 测试
chore: 构建/工具
```

## 扩展开发指南

### 1. 添加新功能

**步骤**：
1. 在 `types` 中定义类型
2. 在 `utils` 中实现工具函数
3. 在 `store` 中添加状态和方法
4. 创建或修改组件
5. 更新文档

**示例：添加数据筛选功能**

```typescript
// 1. 定义类型
interface FilterConfig {
  field: string
  operator: 'eq' | 'ne' | 'contains'
  value: any
}

// 2. 工具函数
export const filterData = (
  data: TableRow[],
  filters: FilterConfig[]
): TableRow[] => {
  // 实现筛选逻辑
}

// 3. Store
actions: {
  setFilters(filters: FilterConfig[]) {
    this.filters = filters
    this.filteredData = filterData(this.tableData, filters)
  }
}

// 4. 组件
<el-input 
  v-model="filterText"
  @input="handleFilter"
  placeholder="筛选..."
/>
```

### 2. 集成后端 API

**配置 Axios**：

```typescript
// src/api/index.ts
import axios from 'axios'

const api = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL,
  timeout: 10000
})

// 拦截器
api.interceptors.response.use(
  response => response.data,
  error => Promise.reject(error)
)

export default api
```

**API 方法**：

```typescript
// src/api/data.ts
import api from './index'

export const saveData = (data: TableRow[]) => {
  return api.post('/data/save', { data })
}

export const loadData = (id: string) => {
  return api.get(`/data/${id}`)
}
```

### 3. 添加单元测试

**推荐工具**：
- Vitest (与 Vite 深度集成)
- Vue Test Utils

**示例测试**：

```typescript
import { describe, it, expect } from 'vitest'
import { analyzeDataTypes } from '@/utils/excelUtils'

describe('excelUtils', () => {
  it('should analyze data types correctly', () => {
    const data = [
      { col_0: 123, col_1: 'text' },
      { col_0: 456, col_1: 'text2' }
    ]
    const columns = [
      { field: 'col_0', title: 'Column1' },
      { field: 'col_1', title: 'Column2' }
    ]
    
    const types = analyzeDataTypes(data, columns)
    
    expect(types['Column1']).toBe('数字')
    expect(types['Column2']).toBe('文本')
  })
})
```

## 性能优化建议

### 1. 大数据量优化

- VXETable 已内置虚拟滚动
- 如需处理 10 万+行，考虑：
  - 分页加载
  - 后端分页
  - Web Worker 处理数据

### 2. 组件懒加载

```typescript
const DataTable = defineAsyncComponent(() => 
  import('./components/DataTable.vue')
)
```

### 3. 防抖和节流

```typescript
import { debounce } from 'lodash-es'

const handleSearch = debounce((value: string) => {
  // 搜索逻辑
}, 300)
```

## 调试技巧

### 1. Vue Devtools

安装 Vue Devtools 浏览器扩展：
- 查看组件树
- 检查 Props 和 State
- 查看 Pinia Store
- 时间旅行调试

### 2. 控制台调试

```typescript
// 在需要调试的地方
console.log('Debug:', { data, columns })
debugger  // 断点
```

### 3. Vite 开发工具

- HMR (热模块替换)
- 错误覆盖层
- 源码映射

---

**祝开发顺利！** 🚀

