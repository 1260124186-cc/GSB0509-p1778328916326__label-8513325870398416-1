# 数据准备模块 - 项目总览

## 📋 项目概述

这是一个功能完整、代码规范的 **数据准备模块**，采用最新的 Vue 3 生态系统构建。该项目展示了如何使用现代前端技术栈构建一个企业级的数据管理应用。

## ✨ 核心功能

### 1️⃣ Excel 文件导入
- 📤 支持 `.xlsx` 和 `.xls` 格式
- 🖱️ 拖拽上传或点击选择
- ✅ 文件格式和大小验证
- 🔄 自动解析表头和数据

### 2️⃣ 数据表格展示
- 📊 基于 VXETable 的强大表格
- 🎨 斑马纹、悬停高亮
- 📏 列宽可调整
- 🚀 虚拟滚动支持大数据量
- ☑️ 复选框多选

### 3️⃣ 数据统计分析
- 📈 实时统计行数、列数
- 🏷️ 显示所有列名（表头）
- 🔍 自动识别数据类型（数字、文本、日期）
- 📊 数据类型分布统计

### 4️⃣ 数据在线编辑
- ✏️ 双击单元格编辑
- ➕ 添加新行
- 🗑️ 删除单行或批量删除
- 💾 实时保存到内存
- 📥 导出为 Excel 文件

## 🛠️ 技术栈

### 核心框架
| 技术 | 版本 | 说明 |
|------|------|------|
| Vue | 3.4+ | 渐进式 JavaScript 框架 |
| TypeScript | 5.3+ | 类型安全的 JavaScript 超集 |
| Vite | 5.0+ | 新一代前端构建工具 |

### UI 组件库
| 技术 | 版本 | 说明 |
|------|------|------|
| Element Plus | 2.5+ | 企业级 UI 组件库 |
| VXETable | 4.5+ | 强大的表格组件 |
| Element Plus Icons | 2.3+ | 官方图标库 |

### 状态管理与路由
| 技术 | 版本 | 说明 |
|------|------|------|
| Pinia | 2.1+ | Vue 官方状态管理库 |
| Vue Router | 4.2+ | 官方路由管理器 |

### 工具库
| 技术 | 版本 | 说明 |
|------|------|------|
| xlsx | 0.18+ | Excel 文件处理 |
| Axios | 1.6+ | HTTP 客户端（预留） |

## 📁 项目结构

```
data-preparation-module/
│
├── 📄 配置文件
│   ├── package.json              # 项目依赖配置
│   ├── vite.config.ts            # Vite 构建配置
│   ├── tsconfig.json             # TypeScript 配置
│   ├── tsconfig.node.json        # Node TypeScript 配置
│   └── .gitignore                # Git 忽略文件
│
├── 📄 文档
│   ├── README.md                 # 项目说明
│   ├── 使用说明.md               # 用户使用手册
│   ├── 开发指南.md               # 开发者指南
│   └── 项目总览.md               # 本文档
│
├── 🎨 入口文件
│   └── index.html                # HTML 入口
│
├── 📂 src/ - 源代码
│   │
│   ├── 🏠 应用入口
│   │   ├── main.ts               # 应用主入口
│   │   ├── App.vue               # 根组件
│   │   └── vite-env.d.ts         # Vite 类型声明
│   │
│   ├── 📂 views/ - 视图层
│   │   └── DataPreparation/      # 数据准备模块
│   │       ├── index.vue         # 主页面（容器组件）
│   │       │
│   │       ├── 📂 components/    # UI 子组件
│   │       │   ├── ExcelImporter.vue    # Excel 导入组件
│   │       │   ├── DataTable.vue        # 数据表格组件
│   │       │   └── DataStatistics.vue   # 数据统计组件
│   │       │
│   │       └── 📂 composables/   # 组合式函数
│   │           └── useTableEdit.ts      # 表格编辑逻辑
│   │
│   ├── 📂 stores/ - 状态管理层
│   │   └── dataPreparation.ts    # 数据准备模块 Store
│   │
│   ├── 📂 types/ - 类型定义层
│   │   └── dataPreparation.ts    # 类型定义
│   │
│   ├── 📂 utils/ - 工具函数层
│   │   └── excelUtils.ts         # Excel 处理工具
│   │
│   └── 📂 router/ - 路由层
│       └── index.ts              # 路由配置
│
└── 📂 .vscode/ - VSCode 配置
    ├── extensions.json           # 推荐扩展
    └── settings.json             # 编辑器设置
```

## 🎯 代码架构设计

### 分层架构

```
┌─────────────────────────────────────────┐
│          视图层 (Views/Components)       │  ← 用户界面
├─────────────────────────────────────────┤
│       组合式函数层 (Composables)         │  ← 业务逻辑
├─────────────────────────────────────────┤
│        状态管理层 (Stores)               │  ← 数据状态
├─────────────────────────────────────────┤
│       工具函数层 (Utils)                 │  ← 通用工具
├─────────────────────────────────────────┤
│        类型定义层 (Types)                │  ← 类型系统
└─────────────────────────────────────────┘
```

### 数据流向

```
┌──────────┐     ┌─────────────┐     ┌────────┐     ┌──────────┐
│   用户   │ --> │   组件事件   │ --> │ Store  │ --> │  视图更新 │
│  操作    │     │             │     │ Action │     │          │
└──────────┘     └─────────────┘     └────────┘     └──────────┘
                                          ↓
                                     ┌────────┐
                                     │  State │ (响应式)
                                     └────────┘
```

### 组件关系

```
DataPreparation (主页面)
    │
    ├── ExcelImporter (导入组件)
    │   └── [上传、验证、解析]
    │
    ├── DataStatistics (统计组件)
    │   └── [展示统计信息]
    │
    └── DataTable (表格组件)
        └── [展示、编辑、操作]
```

## 🚀 快速开始

### 1. 安装依赖

```bash
npm install
```

### 2. 启动开发服务器

```bash
npm run dev
```

访问: `http://localhost:3000`

### 3. 构建生产版本

```bash
npm run build
```

### 4. 预览生产版本

```bash
npm run preview
```

## 📝 开发规范

### 命名规范
- **组件**: `PascalCase` - `DataTable.vue`
- **文件夹**: `camelCase` - `dataPreparation`
- **函数/变量**: `camelCase` - `handleImport`
- **常量**: `UPPER_SNAKE_CASE` - `MAX_FILE_SIZE`
- **类型/接口**: `PascalCase` - `TableColumn`

### 代码风格
- ✅ 使用 TypeScript 严格模式
- ✅ 使用 Composition API (`<script setup>`)
- ✅ 使用 `const` 和 `let`，避免 `var`
- ✅ 函数优先使用箭头函数
- ✅ 组件 Props 和 Emits 类型化

### 文件组织
- ✅ 单一职责原则
- ✅ 逻辑与视图分离
- ✅ 可复用逻辑提取为 Composables
- ✅ 工具函数独立管理

## 🔑 核心模块说明

### 1. ExcelImporter.vue - 导入组件

**功能**:
- 文件上传（拖拽/点击）
- 文件验证（格式、大小）
- 调用解析工具
- 更新全局状态

**关键代码**:
```typescript
const handleImport = async () => {
  const { data, columns } = await readExcelFile(currentFile.value)
  store.setTableData(data, columns, fileName)
}
```

### 2. DataTable.vue - 表格组件

**功能**:
- 数据展示（VXETable）
- 单元格编辑
- 行操作（增删）
- 数据导出

**关键配置**:
```typescript
:edit-config="{ trigger: 'dblclick', mode: 'cell' }"
:checkbox-config="{ checkField: 'checked' }"
```

### 3. DataStatistics.vue - 统计组件

**功能**:
- 基础信息展示
- 列信息展示
- 数据类型分布

**计算属性**:
```typescript
const typeDistribution = computed(() => {
  // 统计各类型数量
})
```

### 4. dataPreparation.ts - Store

**功能**:
- 管理表格数据和列配置
- 提供数据操作方法
- 生成统计信息

**核心方法**:
- `setTableData()` - 设置数据
- `updateCell()` - 更新单元格
- `addRow()` / `deleteRow()` - 行操作
- `updateStatistics()` - 更新统计

### 5. excelUtils.ts - Excel 工具

**功能**:
- `readExcelFile()` - 读取 Excel
- `exportToExcel()` - 导出 Excel
- `analyzeDataTypes()` - 分析数据类型

**技术实现**:
- 使用 `xlsx` 库解析
- FileReader API 读取文件
- Blob 下载文件

## 🎨 UI 设计特点

### 响应式布局
- 移动端：单列布局
- 平板：1:2 布局
- 桌面：1:3 布局

### 主题色系
- Primary: `#409eff` (蓝色)
- Success: `#67c23a` (绿色)
- Warning: `#e6a23c` (橙色)
- Danger: `#f56c6c` (红色)
- Info: `#909399` (灰色)

### 交互设计
- 拖拽上传
- 双击编辑
- 悬停高亮
- 加载状态反馈
- 确认对话框

## 📊 性能优化

### 1. 虚拟滚动
VXETable 内置虚拟滚动，轻松处理 10 万+行数据

### 2. 组件懒加载
路由级别的代码分割：
```typescript
component: () => import('@/views/DataPreparation/index.vue')
```

### 3. 响应式优化
- 使用 `computed` 缓存计算结果
- 避免不必要的渲染
- 合理使用 `v-show` vs `v-if`

## 🧪 测试建议

### 单元测试
- 工具函数测试（`excelUtils.ts`）
- Store 逻辑测试
- 组件渲染测试

### 集成测试
- 导入流程测试
- 编辑流程测试
- 导出流程测试

### E2E 测试
- 完整用户流程测试

## 🔮 扩展方向

### 功能扩展
- [ ] 数据筛选和搜索
- [ ] 数据排序
- [ ] 列的显示/隐藏
- [ ] 数据校验规则
- [ ] 批量操作（替换、填充）
- [ ] 撤销/重做
- [ ] 数据可视化（ECharts）

### 技术扩展
- [ ] 后端 API 集成
- [ ] 数据持久化（数据库）
- [ ] 用户权限管理
- [ ] 多工作表支持
- [ ] 导出多种格式（CSV、JSON）
- [ ] 实时协同编辑（WebSocket）

## 📚 相关文档

1. **README.md** - 项目基本信息和快速开始
2. **使用说明.md** - 详细的用户使用手册
3. **开发指南.md** - 深入的开发者指南
4. **项目总览.md** (本文档) - 全面的项目概览

## 👥 开发团队

如需技术支持或合作开发，欢迎联系。

## 📄 许可证

本项目仅供学习和参考使用。

---

**感谢使用本项目！** 🎉

如有问题或建议，欢迎提 Issue 或 PR。

